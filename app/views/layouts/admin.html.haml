!!!
%html
  %head
    = render 'shared/head', css_file: 'admin', js_file: 'admin'
  %body
    .navbar.navbar-dark.fixed-top.bg-dark.flex-md-nowrap.p-0.shadow.py-2
      = link_to('JobList', root_path, class: 'navbar-brand col-sm-3 col-md-2 mr-0')
      %input.form-control.form-control-dark.w-100{type: 'text', placeholder: 'Search'}
      %ul.navbar-nav.px-3
        %li.nav-item.text-nowrap
          = link_to('Sign out', '',class:'nav-link')
    .container-fluid
      .row
        %nav.col-md-2.d-none.d-md-block.bg-dark.sidebar
          .sidebar-sticky
            %ul.nav.flex-column
              - if can?(:read, Employee)
                %li.nav-item
                  = active_link_to(admin_employees_path, class: 'nav-link') do
                    Pracownicy
              - if can?(:read, User)
                %li.nav-item
                  = active_link_to(admin_users_path, class: 'nav-link') do
                    Użytkownicy
              - if can?(:read, Job)
                %li.nav-item
                  = active_link_to(admin_jobs_path, class: 'nav-link') do
                    Oferty pracy
              - if can?(:read, Technology)
                %li.nav-item
                  = active_link_to(admin_technologies_path, class: 'nav-link') do
                    Technologie
              - if can?(:read, Benefit)
                %li.nav-item
                  = active_link_to(admin_benefits_path, class: 'nav-link') do
                    Benefity
              - if can?(:read, Category)
                %li.nav-item
                  = active_link_to(admin_categories_path, class: 'nav-link') do
                    Kategorie
              - if can?(:read, Pricing)
                %li.nav-item
                  = active_link_to(admin_pricings_path, class: 'nav-link') do
                    Cennik
        %main.col-md-9.ml-sm-auto.col-lg-10.px-4
          = yield