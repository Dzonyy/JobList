.nested-fields
  .nested-fields_inputs
    = f.input :technology_id, as: :select, collection: Technology.all
    = f.input :rating
  .nested-fields_nav
    = link_to_remove_association "Usuń sekcje", f, class: 'btn btn-red btn-sm'
