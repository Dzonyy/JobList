:ruby
  title(t("resource.#{singular_name}.resources_list"))


.row.mb-3.align-items-center.justify-content-between
  .col
    = t("resource.#{singular_name}.name")
  .col
    = render 'admin_buttons'

= render 'flash'

.widget
  .widget-body
    = render 'list'
- if resources.respond_to?(:total_pages)
  .table-pagination
    = will_paginate(resources)